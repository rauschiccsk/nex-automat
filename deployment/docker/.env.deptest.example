# DEPTEST Stack Environment Variables
# Copy to .env.deptest and fill in actual values
# Usage: docker compose -f docker-compose.deptest.yml --env-file .env.deptest up -d

# PostgreSQL
POSTGRES_PASSWORD=your_secure_password_here

# Optional: Override default ports (DEPTEST defaults: +100 offset from ICC)
# POSTGRES_PORT=5533
# TEMPORAL_PORT=7334
# TEMPORAL_UI_PORT=8182
