# PostgreSQL password for Temporal database
POSTGRES_PASSWORD=your_secure_password_here

# Optional: FastAPI URL for invoice-worker (default: http://host.docker.internal:8001)
# FASTAPI_URL=http://host.docker.internal:8001
