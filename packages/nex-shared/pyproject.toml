[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "nex-shared"
version = "0.1.0"
description = "Shared NEX Genesis Btrieve models and utilities"
requires-python = ">=3.9"
dependencies = [
    "pyyaml>=6.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["nex_shared"]
