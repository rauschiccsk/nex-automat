[project]
name = "nex-config"
version = "1.0.0"
description = "Centrálna konfigurácia pre NEX Automat ekosystém"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
