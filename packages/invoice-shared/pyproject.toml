[project]
name = "invoice-shared"
version = "0.1.0"
description = "Shared utilities for invoice processing projects"
requires-python = ">=3.11"
authors = [
    {name = "Zoltán Rausch", email = "rausch@icc.sk"}
]

dependencies = [
    "asyncpg>=0.29.0",
    "pydantic>=2.0.0",
]

[tool.hatch.build.targets.wheel]
packages = ["invoice_shared"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.black]
line-length = 100
target-version = ["py311"]

[tool.ruff]
line-length = 100
target-version = "py311"
