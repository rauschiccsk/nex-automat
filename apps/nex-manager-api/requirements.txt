# NEX Manager API - Dependencies
# FastAPI backend for NEX Manager Electron desktop application
# Python 3.11+

# WEB FRAMEWORK
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.7.0

# AUTHENTICATION
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0,<5.0.0  # passlib not yet compatible with bcrypt 5.x
python-multipart>=0.0.12

# DATABASE
pg8000>=1.29.0  # Pure Python PostgreSQL driver (32-bit compatible)

# HTTP CLIENT (MuFis webhook)
httpx>=0.27.0

# ENVIRONMENT
python-dotenv>=1.0.1

# YAML (module registry)
pyyaml>=6.0

# Fast JSON encoder — used by ORJSONResponse for large list responses
# (e.g. PAB partner catalog with 250k+ rows). 5-10x faster than stdlib json.
orjson>=3.10.0

# NEX CONFIG (centralized configuration)
# Local dev: pip install -e ../../packages/nex-config
# Docker: installed via Dockerfile COPY + pip install
